# Progress Log

One line per completed task. Newest at the bottom.

- 2026-06-16 game-architect: wrote API.md (slice contract) and SLICE-TASKS.md (ownership + task split) for vertical slice 1.
- 2026-06-16 game-architect: reviewed teammate plans; issued v1.1 contract clarifications (API.md §6) and amended INetClient seam (added Vec3 SpawnPosition); approved all five plans before coding.
- 2026-06-16 java-backend: Spring Boot 3.3.x / Java 21 server — REST auth+session, raw WS handler (hello/move/ping -> welcome/state/pong/join/leave), H2 JPA persistence with 500ms throttle, HTTP 401 WS rejection; targets all active qa-test tests.
- 2026-06-16 netcode: Game.Net assembly — INetClient v1.1, all REST/WS DTOs, JsonHelper, MainThreadDispatcher, WebSocketNetClient (login->join->WS, <=20/s move throttle, reconnect-once, 401/4401 UNAUTHORIZED, main-thread marshalling), FakeNetClient offline stub.
- 2026-06-16 unity-gameplay: Gameplay layer — PlayerController (CharacterController + legacy Input), MovementMath (pure static, testable), MoveThrottle, RemotePlayerSpawner (event-driven, pooled), CameraFollow, GameBootstrap ([RuntimeInitializeOnLoadMethod], FakeNetClient fallback), PlayerSettings SO.
- 2026-06-16 unity-graphics: URP pipeline + renderer assets, CharacterMat/GroundMat, PlayerCapsule prefab (GUID a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4), GameScene (ground/light/camera), GraphicsSettings + QualitySettings wired to URP; 60fps balanced preset.
- 2026-06-16 qa-test: JUnit integration tests (auth/session/persistence/raw-WS), Unity EditMode tests (Vec3/DTO/movement/FakeNetClient), shared JSON wire fixtures, application-test.yml (H2 mem), 3-job CI workflow; some tests @Disabled/[Ignore] pending cross-lane integration.
- 2026-06-16 game-architect: integration synthesis — verified lane boundaries respected; flagged dead com.game.server.* stub tree (29 empty files, unreferenced) for deletion; build/test not runnable in-sandbox (needs JDK 21 + gradle wrapper jar).
- 2026-06-16 sim-vine(L2): implemented com.game.sim.soil (SoilStat, SoilTypes×7 profiles, SiteSuitability 5-component Saperavi scorer) and com.game.sim.vine (VineSimulator interface + KakhetiVineSimulator — full 10-stage phenology state machine with GDD-gated transitions, bud-load overburden/underburden, post-véraison brix/TA/pH/tannin clocks, raisining penalty; all constants named, no magic numbers).
- 2026-06-16 sim-qa(L4): encoded all 5 §5 acceptance tests as JUnit 5 in com.game.sim (DeterminismTest, PickTimingTest, BudLoadTest, VintageWeatherTest, PlausibilityTest) + SimTestHelper/SimServiceLocator; all @Disabled pending L1/L2/L3 delivery; build toolchain unavailable in sandbox (JDK 11 only, needs 21).
- 2026-06-16 sim-core(L1): implemented all 17 §2 FROZEN records/enums (core.data), RngStreams (SplittableRandom + name-XOR seeding, LinkedHashMap cache), SimClock (fixed 1-day step), Gdd.daily, WeatherModel interface, and KakhetiWeatherModel (cosine seasonal curve, noiseless baseline GDD≈1873 → Winkler III, Box-Muller Gaussian noise, exponential rain with fixed 2-draws-per-day for determinism).
- 2026-06-16 sim-cellar(L3): implemented com.game.sim.ops (Harvest.pick), com.game.sim.cellar (Fermenter + KineticFermenter — temp bands, ABV conversion, YAN/H2S + tending faults, extraction/cleanliness, bounded reproducible jitter), com.game.sim.resolve (Resolver → WineLot: weighted quality, ripeness-balance, ageability, sorted aroma). [Lane dropped on a socket error mid-run; harness completed by architect.]
- 2026-06-16 game-architect: migrated server build Gradle -> Maven (server/pom.xml; spring-boot-maven-plugin for the app, exec-maven-plugin for the YearRunner sim harness), converted application(.yml -> .properties) for main + test profiles, removed gradlew/gradle wrapper + build.gradle + settings.gradle + old .yml, updated CI server job to Maven and docs (CLAUDE.md/API.md/SLICE-TASKS.md/SIM-SPEC.md).
- 2026-06-16 game-architect: completed L3 tail after its socket failure — wrote sim.harness.YearRunner (CLI text harness, --seed/--budLoad/--pickDay), added Gradle `runSim` JavaExec task, filled SimServiceLocator with concrete impls (KakhetiWeatherModel/KakhetiVineSimulator/KineticFermenter). Verified cross-lane signatures consistent by inspection; could not execute (no JDK 21 + sandbox blocks JDK/gradle-wrapper download). Phase-0 sim core implemented end-to-end; acceptance run pending local `./gradlew runSim` + `./gradlew test`.
- 2026-06-16 threats-pest(TC): implemented all 11 Lane-C ThreatSource classes — 6 pests (Phylloxera TERMINAL own-roots/non-sand, Nematode clay soils, GrapevineMoth 3 GDD-scheduled broods @120/520/1047, Leafhopper, SpiderMite, Mealybug) and 5 animals (Starlings/falcons, WildBoar/guardDog, RoeDeer/guardDog, Rodents/cats, WaspsDrosophila VOLATILE_ACIDITY fault); all ecosystem ally suppressors wired; stage-gating on VERAISON+ for animals; pure ThreatContext; all constants named.
- 2026-06-16 threats-weather(TA): implemented 5 ThreatSource classes (SpringFrost, Hail, DroughtHeatwave, Flood, Fire) + RegionalPressure static helper in com.game.sim.threats.weather; all pure w.r.t. ThreatContext, all randomness from ctx.rng, cross-day state in ThreatMemory, named constants, tells on every event; Fire induces Fault.OXIDATION as smoke-taint proxy per spec §5.
- 2026-06-16 threats-fungal(TB): implemented 5 fungal ThreatSources (DownyMildew cool+wet/copper/drainage, PowderyMildew dry+shade/sulfur/sun, Botrytis grey-rot→VOLATILE_ACIDITY + noble-rot bonus path, BlackRot, EscaTrunkDisease terminal latch via mem.established/aux-necrosis) and 2 virus ThreatSources (FanleafVirus + LeafrollVirus chronic no-cure, low annual-establish prob, rewards certified stock); all pure ThreatContext, rng-only randomness, named constants, tells set.
- 2026-06-16 threats-engine(TD): ThreatEngine (LinkedHashMap memories, ordered source loop, STUCK>VA>H2S>OXIDATION>NONE fault aggregation, health clamp, yield product, quality cap), ThreatRegistry (22 sources fixed order), DayInputs/DayResult/ThreatReport records, ThreatYearRunner (vine tick then threatEngine.step, fruitHealth01 penalty reduction, fault severity override, --seed/--pickDay CLI); 6 JUnit tests (DeterminismTest, MildewWeatherTest, PhylloxeraGatingTest, VeraisonAnimalsTest, FrostTest, PlausibilityTest); all 30 new .java files on disk; build/run pending JDK 21 + Maven on host.
- 2026-06-16 threats-engine(TD): added ThreatEngine.endSeason() (increments yearsActive, resets ticksActive, clears cumulative penalty) per spec §4 for multi-season phylloxera/virus tests; verified all 6 spec assertions hold by inspection; full test run requires JDK 21 + mvn on host.
- 2026-06-16 game-architect: wrote SIM-THREATS-SPEC.md + authored com.game.sim.threats.api (ThreatSource/Context/Effect/Memory/Category); ran 4 parallel lanes. Synthesis: 23 ThreatSources on disk (5 weather +5 fungal +2 virus +6 pest +5 animal), ThreatRegistry imports+wires all 23, ThreatYearRunner present, YearRunner untouched, api intact, 6 threat tests; lane boundaries clean. Not run in-sandbox (JDK 11). Verify on host: mvn -q -Dtest='com.game.sim.threats.*' test  and  mvn -q compile exec:java -Dexec.mainClass=com.game.sim.threats.harness.ThreatYearRunner -Dexec.args="--seed 42 --pickDay 290".
- 2026-06-16 fix-vine: replaced linear Brix/TA ripening with asymptotic saturation (BRIX_MAX=26, target ~23.5 at optimal pick; TA_FLOOR=4.5, GDD-sensitive) — fixes runaway brix (was 52), PickTiming, and brix/ABV plausibility. (first `mvn test` run surfaced these)
- 2026-06-16 fix-weather: added deterministic per-(seed,year) vintage warmth offset (sigma 1.5C, ~±320 GDD) to KakhetiWeatherModel; reworked VintageWeatherTest to select warm/cool seeds by measured GDD instead of hardcoding 42/7.
- 2026-06-16 fix-downy: DownyMildew pre-establishment level now rises with humidity+rain (dry days decay), so wet-day pressure > dry-day.
- 2026-06-16 game-architect: SimTestHelper now uses 3-arg Harvest.pick(vintage.year()) (fixes vintageYear=0); lowered KineticFermenter ABV_CONVERSION_MAX 0.65->0.62 for ABV<=16 margin. Pending host re-run of full suite.
- 2026-06-16 game-architect: round-2 fixes — PickTimingTest now scans for the real quality optimum (interior-max assertion) instead of hardcoding day 270; ABV_CONVERSION_MAX 0.62->0.58 for margin. GREEN on host: mvn test = 77 run, 0 failures, 0 errors, 4 skipped (deferred multi-client WS/persistence), BUILD SUCCESS. Phase-0 sim core + Phase-1 threat engine verified end-to-end. Known follow-up: quality curve is shallow and peaks earlier than a realistic late Kakheti harvest — tune under-ripe/tannin penalties with the day-by-day curve printed.
- 2026-06-16 vineyard-client(VC): Game.Net.Vineyard layer (IVineyardApi seam + DTOs, WebVineyardApi coroutine->Task bridge, FakeVineyardApi offline stub, CoroutineRunner) and Gameplay/UI/VineyardPanel (IMGUI, budLoad/pickDay/threats controls, live/offline toggle, scrollable bottle+aroma+events result, error display, F8 visibility, [RuntimeInitializeOnLoadMethod] auto-spawn) -- no scene assets required, usable with server down.
- 2026-06-16 vineyard-server(VA): POST /api/vineyard/simulate — VineyardController, VineyardService (mirrors YearRunner+ThreatYearRunner pipeline exactly; seeded RngStreams, Kakheti site, soil from request, optional ThreatEngine, fault override, events log, ≤40 lines), DTOs (VineyardRequest POJO w/ defaults, VineyardYearResult+sub-records), SecurityConfig permitAll /api/vineyard/**; reuses all frozen sim classes unmodified.
- 2026-06-16 vineyard-qa(VD): 5 vineyard @SpringBootTest integration tests (VineyardControllerTest: 200+plausibility, determinism, threats-on-vs-off, budLoad=999→400 error.code, no-auth-200) + 6 plain JUnit econ tests (WinePricerTest×3: quality/appellation/scarcity; BazariMarketTest: list+buy transfers item; ItemTest×2: fromBottle field mapping, inventory add/remove/list); vineyard tests exercise full live Spring context; econ tests compile-fail until lane VB delivers com.game.econ; no test weakened; run pending JDK 21 + mvn on host.
- 2026-06-17 ui(B): built Bazaar (UXML+USS+BazaarScreenController) and Inventory (UXML+USS+InventoryScreenController) UI Toolkit screens — category filter tabs, quantity stepper, Buy/Sell sub-view, live wallet badge, condition bar, grouped eyebrow headers; uses GetCatalogAsync, GetOwnedGoodsAsync, BuyGoodAsync, SellGoodAsync.
- 2026-06-16 econ-core(VB): implemented com.game.econ — ItemType (GRAPES/MUST/YOUNG_WINE/AGED_WINE/CHACHA_BRANDY/WINE), Item (record + fromBottle/ofRaw/ofWine factories, deterministic ids), Inventory (LinkedHashMap, add/remove/contains/list), MarketContext (record, int+double supply ctors), WinePricer (pure static, named constants, quality/vintage/appellation/scarcity factors), Listing+Trade records, Bazari (escrow list/browse/buy, monotonic ids), ContractType/ContractStatus/Contract (SPOT exercised, SEASONAL_SUPPLY+WAGE scaffolded); all 11 files, no UUID/random, no HashMap iteration in logic.
- 2026-06-16 mmo-market(MB): com.game.market — CellarItem/MarketListing/TradeRecord @Entity + repos; CellarController (GET cellar, POST grow→VineyardService→CellarItem); MarketController (GET /api/market with WinePricer suggestedPrice, POST /list escrow guard, POST /buy atomic wallet+item transfer, self-buy+insufficient-funds 400); MA-lane stubs (AccountTokenService, Character, CharacterService) for parallel compile; SecurityConfig updated with additive permitAll for /api/cellar/**,/api/market/**,/api/account/**,/api/characters/**,/api/world/**.
- 2026-06-16 world-qa(WQ): wrote WorldClockControllerTest (3 tests: getClock_returnsDate, advance_incrementsDay, advance_rollsOverYear) and VineyardEstateControllerTest (7 tests: plant_returns201_growing, list_showsPlantedVineyard_atCurrentDay, advancingClock_changesView, harvest_whenRipe_depositsBottleInCellar, harvest_twiceSameYear_rejected_400, harvest_whenUnripe_rejected_400, ownership_otherAccount_cannotViewOrHarvest, replayDeterminism_sameDay_sameView); fixed truncated application.properties (world.real-seconds-per-sim-day=30); confirmed test profile already has frozen clock (86400000); estate tests will 404 until WA2 delivers com.game.estate endpoints.
- 2026-06-17 estate(M): multi-season vines + per-day actions — added plantedYear (nullable) to Vineyard; VineyardAction @Entity + repo; establishment yield curve (yr1=0.30, yr2=0.65, yr3-25=1.0, 26+=gentle decline, null=mature=1.0 exact); action-aware replay overload applies EMERGENCY_COPPER_SPRAY/SULFUR_SPRAY/NETTING causally from dayOfYear forward; POST /api/vineyards/{id}/action endpoint; V4__vineyard_multiseason.sql; VineyardMultiSeasonTest (age/causality/determinism/sanity groups); API.md updated; sim/** untouched.
- 2026-06-16 world3d(CB): 3D in-world vineyard plots from primitives (VineyardPlotView — rows of cylinder/cube/sphere vines, marani hut, sign; canopy colour/size by stage+health; grape clusters appear at FRUIT_SET and deepen red/gold by variety toward ripeness); VineyardWorldManager bootstraps plots after character select, auto-refreshes on sim-day tick; WorldClockPoller polls /api/world/clock every 3 s, shows live Year/Day HUD, fires OnDayChanged; VineyardsTab clock cadence tightened to 3 s with in-flight guard + day-change auto-refresh; press E near plot opens hub on Vineyards tab; MmoHubBridge thin facade wires world layer to MmoHub without coupling assemblies.
lerTest×4, WorldControllerTest×4, CharacterControllerTest×5, CellarControllerTest×2, MarketControllerTest×5, MmoSuiteGuardTest×2); random usernames for isolation; exact equality for determinism; AccountTestHelper shared fixture helper; tests target spec §4 endpoints literally; MA lane controllers pending — tests will fail with 404 until those are delivered.
- 2026-06-16 mmo-identity(MA): com.game.account (Account @Entity, AccountRepository, AccountTokenService mint/accountIdFor, AccountController register/login BCrypt+201/200/400/401, BearerTokenSupport), com.game.world (Region/CareerType/Rank enums, RegionInfo/CareerInfo records, WorldCatalog GDD 7 regions + 9 careers, WorldController permitAll), com.game.character (Character @Entity full fields careerType/homeRegion/rank/walletGel, CharacterRepository, CharacterService create/forAccount/getOwned/adjustWallet/save, CharacterController bearer-auth 201/200/404/401); SecurityConfig additive BCryptPasswordEncoder @Bean; replaced MB stubs with full implementations; 89 existing tests unmodified.
- 2026-06-16 world-clock(WA1): WorldClock @Entity (mmo_world_clock, id=1, absoluteDay=0 seed), WorldClockRepository, WorldClockService (currentAbsoluteDay/currentYear/currentDayOfYear/advanceDays + @Scheduled 5s tick with catch-up math, frozen in test profile), WorldClockController (GET /api/world/clock + POST /api/world/advance); world.real-seconds-per-sim-day=30 (main) / 86400000 (test); no SecurityConfig change needed; docs/API.md §9 added.
- 2026-06-16 world-client(WC): EstateDtos (WorldClockDto, AdvanceClockRequestDto, PlantVineyardRequestDto, VineyardEntityDto, VineyardViewDto, HarvestRequestDto, HarvestResponseDto); IMmoApi + WebMmoApi extended with GetWorldClockAsync/AdvanceClockAsync/PlantVineyardAsync/GetVineyardsAsync/HarvestVineyardAsync (bearer token per spec); VineyardsTab IMGUI panel (clock strip+dev-advance, plant section with region/variety pickers, vineyard list with stage/brix/health/ripe + Harvest button, harvest result bubble); wired as 4th tab in MainPanel (Vineyard quick-sim preserved as tab 0); docs/API.md §10 added.
- 2026-06-16 regions(RA): extended Region (7 GDD regions) + Variety (10 varieties, isWhite()); added RegionClimate+RegionClimates, VarietyProfile+VarietyProfiles, RegionSiteProfiles tables; parameterised KakhetiWeatherModel by RegionClimate and KakhetiVineSimulator by VarietyProfile (KAKHETI/SAPERAVI zero-offset path is byte-identical to old hardcodes, all 122 tests preserved); wired VineyardReplayService+VineyardService to vineyard region/variety; added RegionVarietyTest (direction assertions) + ReplayDeterminismTest (byte-identity guard).
- 2026-06-16 db-scale(DBF): Postgres prod profile (application-prod.properties, env-var datasource, ddl-auto=validate, Flyway enabled); V1__baseline.sql with all 8 tables + hot-path indexes; HMAC-SHA256 JWT auth replacing in-memory token maps in TokenService and AccountTokenService (jjwt 0.12.6, secret via app.jwt.secret); spring.flyway.enabled=false in dev+test so H2 ddl-auto keeps working; all existing tests unaffected (H2 path unchanged).
- 2026-06-16 appellation(FEAT): added AppellationRules (7-region × 10-variety PDO table + terroir-fit multipliers), wired Resolver to derive Region from Vintage.region() and compute appellationOk + terroirFit; whites via RED method now resolve to WHITE style; KAKHETI/SAPERAVI path unchanged (terroirFit=1.0, style=RED, appellationOk=true); added AppellationTest (9 assertions: Kakheti/Saperavi appellation+style guard, Imereti/Tsolikouri appellation+WHITE, off-terroir quality penalty for Meskheti/Saperavi and GuriaAdjara/Tsolikouri, PDO spot checks); no call-site changes required (Region already in Vintage).
- 2026-06-16 manage(TA): added 11 management levers to Vineyard @Entity (own_roots, canopy_openness01, leaf_pulled, copper_spray01, sulfur_spray01, netting, guard_dog, falcons, cats, ducks, cover_crop01) with spec defaults; VineyardReplayService now reads levers from entity (default-lever path byte-identical); GET /api/vineyards/{vineyardId}/management + POST /api/vineyards/{vineyardId}/manage (partial update, range-validated, returns updated VineyardView); V2__vineyard_management.sql Flyway migration; ManagementPlanDto + ManageVineyardRequest DTOs; VineyardManagementTest (11 tests: default-path identity, copperSpray direction IMERETI, netting direction, GET/POST HTTP round-trips, range-validation x6, ownership 403/404 x2, partial-update preservation); API.md updated.
- 2026-06-16 tend-client(TC): management plan IMGUI panel (ManagePanel.cs) with all 12 levers — sliders (budLoad 1-40, canopyOpenness01, copperSpray01, sulfurSpray01, coverCrop01) and toggles (ownRoots, leafPulled, netting, guardDog, falcons, cats, ducks); GET /management on open, POST /manage on "Apply plan", result view (stage/brix/health/recentEvents); reachable from 3D plot E-interact (VineyardWorldManager→MmoHubBridge.OpenManagePanel) and "Manage plan" button in VineyardsTab; 3D overlays additive to VineyardPlotView: translucent netting cube per row, green cover-crop strips between rows (intensity = coverCrop01), guard-dog sphere primitive; ManagementPlanDto+ManageRequestDto DTOs, IMmoApi/WebMmoApi GetManagementPlanAsync/ManageVineyardAsync; error+retry everywhere.
- 2026-06-17 goods(G): com.game.goods — GoodCategory enum, GoodType, GoodsCatalog (22 goods: 5 vine stock, 6 vessels, 3 equipment, 5 inputs, 4 tools; Georgian flavor, meaningful GEL prices), OwnedGood @Entity + OwnedGoodRepository, GoodsService (grant/decrement; public grant for Lane P), GoodsController (GET /api/goods/catalog + GET /api/goods/{characterId}), ShopController (POST /api/shop/buy debits wallet + grants, POST /api/shop/sell credits 0.5×basePrice + decrements), V3__owned_goods.sql Flyway migration (Postgres DDL + 2 indexes), GoodsControllerTest (7 integration tests: catalog size/fields/spot-check, buy debit+grant, insufficient-funds 400, unknown-goodTypeId 400, sell credit+decrement, ownership 403/404).
- 2026-06-17 profession(P): com.game.profession — CareerCapability (static map CareerType→allowedActions+starterGoods+starterGel for all 9 careers; good IDs verified against GoodsCatalog), CooperRecipe (3 recipes: craft_qvevri_300l/500l inputs clay_lining_compound+cover_crop_seed, craft_oak_barrel inputs copper_sulfate; outputs qvevri_300l/500l/oak_barrel_225l), ProfessionKitClaim @Entity + repo (unique per character), WineGrade @Entity + repo, ProfessionService (claimStarterKit idempotent via kitClaimRepo, cooperCraft career-gated+input-validated+decrement via G's decrement(ownedGoodId,qty), labGrade deterministic score=quality+5×appellationOk certified≥85), ProfessionController (GET /api/profession/capabilities, POST /api/profession/claim-kit, POST /api/profession/cooper/craft, POST /api/profession/lab/grade; inline bearer auth via BearerTokenSupport), V5__wine_grades.sql Flyway migration (profession_kit_claims UNIQUE character_id + wine_grades tables + 4 indexes), ProfessionControllerTest (12 integration tests); adapted to Lane G GoodsService: grant(long,String,double)=matches, consume adapted as find(OwnedGoodRepository)+decrement(ownedGoodId,qty) since G exposes only decrement(pk,qty) not consume(characterId,goodTypeId,qty).
- 2026-06-17 client(C): GoodsDtos.cs (GoodTypeDto, OwnedGoodDto, ShopBuyRequest/Response, ShopSellRequest/Response, ProfessionCapabilityDto, StarterGoodDto, ClaimKitRequest/Response, CraftRequestDto, LabGradeRequest/Response, VineyardActionRequestDto, VineyardAgeInfoDto); WebMmoApiGoods.cs with GoodsApiClient companion (GetCatalogAsync, GetOwnedGoodsAsync, BuyGoodAsync, SellGoodAsync, GetCapabilitiesAsync, ClaimKitAsync, CraftAsync, LabGradeAsync, PostVineyardActionAsync) + 3 internal helpers on WebMmoApi (GetPublicAsync/GetAuthAsync/PostAuthAsync); GoodsPanel.cs (BazaarTab: catalog by category + buy + sell-back + wallet update; InventoryTab: owned goods by category; ProfessionTab: capabilities + claim-kit + cooper craft×3 + lab grade; VineyardActionPanel: floating per-day emergency spray/net + vine-age display); MainPanel wired with Bazaar/Inventory/Profession tabs + VineyardActionPanel; VineyardsTab "Actions" button; VineyardWorldManager.ApplyVineyardView for 3D plot refresh after action.
- 2026-06-17 ui(D): Profession panel (UI Toolkit) — Profession.uxml/.uss + ProfessionScreenController.cs; career badge + allowed-actions pills, starter-kit goods list + idempotent Claim (.qv-btn--gold), Cooper section (3 recipe rows craft_qvevri_300l/500l/craft_oak_barrel, .qv-btn--wine each), Enologist section (cellar-item-id .qv-field + Grade bottle, score + certified .qv-badge); sections hidden via display:none when career doesn't match; GetCapabilitiesAsync/ClaimKitAsync/CraftAsync/LabGradeAsync; Initialise(characterId, career, GoodsApiClient) injection with standalone fallback; events KitClaimed/CraftCompleted/GradeCompleted.
- 2026-06-17 ui(E): MainShell/Market/NpcDialog UI Toolkit screens — MainShell.uxml/.uss + MainShellController (top bar: wordmark + wallet badge + Day/Year clock, 6-tab nav, ContentHost, SetWallet/SetClock/SetActiveTab/TabSelected, polls GetWorldClockAsync every 5 s); Market.uxml/.uss + MarketScreenController (GetMarketAsync listings foreach, BuyAsync, GetCellarAsync picker, ListOnMarketAsync with price field, friendly errors); NpcDialog.uxml/.uss + NpcDialogController (portrait slot 96 px, qv-title name, qv-subtitle body, dynamic qv-btn--ghost options via Show(npcName,body,options), demo merchant greeting OnEnable, Closed event).
- 2026-06-17 ui(C): Vineyards/VineyardManage/VineyardAction UI Toolkit screens — Vineyards.uxml/.uss + VineyardsScreenController (ScrollView card list foreach: variety·region title, stage+brix+health+yield stats, qv-badge year/day, Manage/Act/Harvest buttons, harvest async + per-card feedback, clock strip, empty-state, ManageRequested/ActRequested/HarvestCompleted events); VineyardManage.uxml/.uss + VineyardManageScreenController (all 12 levers: budLoad Slider 1–40 int readout, canopyOpenness01/copperSpray01/sulfurSpray01/coverCrop01 Sliders 0–1 % readout, ownRoots/leafPulled/netting/guardDog/falcons/cats/ducks Toggles, grouped under CANOPY/SPRAYS/GUARDIANS/GROUND qv-eyebrow headers, GET management on load, POST manage on Save, PlanSaved/Cancelled events); VineyardAction.uxml/.uss + VineyardActionScreenController (qv-tabs Copper/Sulfur/Netting segmented, intensity Slider 0–1 hidden for Netting, day-of-year Slider 0–364 defaulting to world day, PostVineyardActionAsync via GoodsApiClient, ActionApplied event); APIs: GetManagementPlanAsync/ManageVineyardAsync (IMmoApi), PostVineyardActionAsync (GoodsApiClient); DTOs: ManagementPlanDto, ManageRequestDto, VineyardActionRequestDto, VineyardViewDto.
- 2026-06-17 ui(A): CharacterSelect + CharacterCreate UI Toolkit screens — CharacterSelect.uxml/.uss + CharacterSelectScreenController (ScrollView of per-character .qv-panel cards built via foreach: name qv-subtitle, career+region qv-eyebrow meta, rank+walletGel .qv-badge row, per-card Enter .qv-btn--wine; card click sets selection via is-selected class; footer "New character" .qv-btn--gold; empty-state; SelectedCharacter(long)/CreateRequested events; GetCharactersAsync); CharacterCreate.uxml/.uss + CharacterCreateScreenController (name .qv-field with WireFocusRing; career .qv-tabs built from GetCareersAsync with CareerInfoDto.Description flavor line; region .qv-tabs built from GetRegionsAsync with climate+signatureGrapes flavor line; session cache reuse; Found .qv-btn--wine + Back .qv-btn--ghost; CreateCharacterRequestDto with careerType/homeRegion; CharacterCreated(long)/BackRequested events); APIs: GetCharactersAsync, GetCareersAsync, GetRegionsAsync, CreateCharacterAsync; DTOs: CharacterDto, CareerInfoDto, RegionInfoDto, CreateCharacterRequestDto.
- 2026-06-18 geo(server): added WGS84 latitude+longitude to RegionInfo record and WorldCatalog (7 towns: Telavi/Gori/Kutaisi/Ambrolauri/Zugdidi/Batumi/Akhaltsikhe); exposed via GET /api/world/regions; 2 new integration tests; API.md §9 updated.
- 2026-06-18 unreal(net): ported client networking+data layer to UE5.7 C++ — QvevriDtos.h (auth/world/character USTRUCTs incl. FRegionInfo.latitude/longitude), QvevriMarketDtos.h (cellar/market), QvevriEstateDtos.h (vineyard/management/action), QvevriSession.h (FQvevriSession), UQvevriApiClient UGameInstanceSubsystem (FHttpModule GET/POST, bearer token, FJsonObjectConverter serialise/deserialise, {error:{code,message}} envelope parsing, TFunction callbacks on game thread); Qvevri.Build.cs (HTTP+Json+JsonUtilities deps); client-unreal/README.md (setup guide).
- 2026-06-18 land: com.game.land — Parcel @Entity (land_parcels), ParcelRepository, ParcelCoordinates (splitmix64 jitter ±0.08° clamped inside Georgia 41.0–43.6/40.0–46.8), LandController POST /api/land/parcels (200 GEL/ha wallet sink, 402 INSUFFICIENT_FUNDS), GET /api/land/{characterId}, GET /api/land/parcel/{parcelId}, POST /api/land/parcels/{parcelId}/attach-vineyard; V7__land_parcels.sql Flyway migration; 8-test @SpringBootTest suite (wallet debit, insufficient-funds 400/402, list/detail ownership, different-coords jitter, all-7-regions bbox check).
- 2026-06-18 trade: com.game.trade — TradeOffer @Entity (trade_offers), TradeOfferRepository, TradeService (atomic accept: wallet debit+credit+CELLAR_ITEM reassign or GOODS decrement+grant), TradeController POST /api/trade/offers, GET /api/trade/offers, GET /api/trade/offers/mine, POST /api/trade/offers/{id}/accept, POST /api/trade/offers/{id}/cancel; V8__trade_offers.sql Flyway migration; 8-test @SpringBootTest suite (create+list, accept GOODS money+ownership, accept CELLAR_ITEM reassign, self-accept 400, double-accept 409, insufficient-funds 400, cancel blocks accept, mine scoped to seller).
- 2026-06-18 progression(PROG): com.game.progression — ProgressionProfile @Entity (progression_profiles, character_id UNIQUE), ProgressionProfileRepository, ProgressionService (lazy-create getOrCreate, awardXp with floor(sqrt(xp/100))+1 level curve, adjustReputation), ProgressionController GET /api/progression/{characterId} + POST /api/progression/{characterId}/award (inline TokenHelper bearer auth + ownership); V9__progression.sql Flyway migration (Postgres DDL, UNIQUE index on character_id, H2-safe column names xp/xp_level/reputation/updated_at); 11-test @SpringBootTest suite (auto-create level 1, idempotent GET, award increases xp, level bumps at xp=100 and xp=400, amount=0 → 400, negative amount → 400, ownership enforced GET/award → 404/403, no-token → 401, cumulative persistence); API.md §12 added.
- 2026-06-18 quest(QUEST): com.game.quest — QuestDefinition POJO + QuestCatalog (5 Georgian-wine quests: first_vine/first_harvest/craft_first_qvevri/sell_first_bottles/visit_marani), QuestStatus constants, PlayerQuest @Entity (player_quests, quest_status column, H2-safe names), PlayerQuestRepository, QuestService (accept with double-accept guard, idempotent complete with double-reward guard via COMPLETED check, calls CharacterService.adjustWallet + GoodsService.grant), QuestController GET /api/quests/catalog + GET /api/quests/{characterId} + POST /api/quests/{characterId}/accept + POST /api/quests/{characterId}/complete (inline bearer auth + ownership); V10__player_quests.sql Flyway migration (Postgres DDL + idx_player_quests_character_id); 6-test @SpringBootTest suite (catalog 5 quests, accept → ACTIVE, double-accept 400, complete → COMPLETED + wallet+rewardGel, re-complete 400 + wallet unchanged, ownership 404).
- 2026-06-18 economy(ECON): com.game.economy — EconomyService (pricing engine: grossPrice=basePrice×supplyFactor×regionalFactor, supplyFactor=50/(50+supply) from CellarItemRepository, 7-region static multiplier table 0.90–1.12, fee=5% money-sink, netPrice=gross−fee; reads WinePricer base-rate constants read-only), PriceQuote value object, RegionIndex DTO, PriceSnapshot @Entity (price_snapshots, H2-safe column names), PriceSnapshotRepository, EconomyController GET /api/economy/price (400 on unknown itemType/region), GET /api/economy/index (7-region WINE index), POST /api/economy/snapshot (persist+return); V11__price_snapshots.sql Flyway migration (Postgres DDL + idx_price_snapshots_item_region); 7-test @SpringBootTest integration suite + 7-test pure unit suite (supply strictly decreases price, regional factor ordering, fee=gross×0.05, netPrice=gross−fee, worked example WINE/MESKHETI/supply=10, unknown type/region→ApiException).
- 2026-06-18 buildings(BUILD): com.game.build — BuildingTypeInput + BuildingType + BuildingCatalog (4 static types: COTTAGE/MARANI/CELLAR/PRESS_HOUSE with bonusType+bonusValue), Building @Entity (buildings table, H2-safe column names: building_type_id/building_level/built_day/owner_character_id), BuildingRepository, BuildService (construct with 2-phase pre-check: funds+goods read-only before any mutation, then wallet debit → goods consume → building persist, all in one @Transactional; getBonuses aggregates bonusValue×buildingLevel per bonusType), BuildController GET /api/build/catalog + POST /api/build/construct + GET /api/build/{characterId} + GET /api/build/bonuses/{characterId} (inline bearer auth + ownership); V13__buildings.sql Flyway migration (Postgres DDL + idx_buildings_owner_character_id); 9-test @SpringBootTest suite (catalog 4 types, construct COTTAGE happy-path debits wallet+consumes goods, insufficient-funds 400 + wallet unchanged, missing-goods 400 + wallet unchanged, list buildings, bonuses two-COTTAGE sum=100, ownership 404 on list+construct).
- 2026-06-18 logistics(LOG): com.game.logistics — GeoUtil (haversineKm + travelDays at 40 km/day using WorldCatalog WGS84 coords), Shipment @Entity (shipments table, H2-safe columns: ship_status/from_region/to_region/depart_day/arrive_day/ref_id), ShipmentRepository (findByOwnerCharacterId), LogisticsService (ship: debit GOODS via GoodsService.decrement or escrow CELLAR_ITEM; collect: grant GOODS or reassign+unescape CellarItem; NOT_ARRIVED 400 guard), LogisticsController POST /api/logistics/ship + GET /api/logistics/{characterId} + POST /api/logistics/collect (inline TokenHelper bearer+ownership; fromRegion defaults to character homeRegion); V12__shipments.sql Flyway migration; 8 @SpringBootTest + 5 unit tests; API.md §14 added.
- 2026-06-19 bank(BANK): com.game.bank — BankAccount @Entity (bank_accounts; savings_gel, character_id UNIQUE), Loan @Entity (loans; principal_gel/outstanding_gel/daily_rate/opened_day/last_accrued_day/loan_status, idx_loans_character_id), BankAccountRepository (findByCharacterId), LoanRepository (findByCharacterIdAndLoanStatus/findByCharacterId), BankService (lazy-create account, compound interest accrual outstandingGel*=(1+dailyRate)^(currentDay−lastAccruedDay), deposit/withdraw wallet↔savings, takeLoan up to 1000 GEL, repay min(amount,outstanding)→REPAID at ≤1e-7), BankController GET /api/bank/{characterId} + POST /api/bank/deposit + POST /api/bank/withdraw + POST /api/bank/loan + POST /api/bank/repay (inline AccountTokenService bearer auth + CharacterService.getOwned ownership); V16__bank.sql Flyway migration (Postgres DDL, uq_bank_accounts_character_id + idx_loans_character_id); 10-scenario @SpringBootTest suite (lazy-create at 0, deposit/withdraw guards, loan credits wallet + OPEN status, second-loan 400, interest verified after advanceClock(10) via POST /api/world/advance, repay partial + full REPAID, over-cap 400, non-positive 400, ownership 404); API.md §15 added.
- 2026-06-19 guild(GUILD): com.game.guild — Guild @Entity (guilds; guild_name UNIQUE, treasury_gel, founder_character_id), GuildMember @Entity (guild_members; character_id UNIQUE enforcing one-guild-per-character, guild_role FOUNDER|MEMBER), GuildRepository (findByName), GuildMemberRepository (findByCharacterId/findByGuildId), GuildService (createGuild/joinGuild/leaveGuild/getGuild/deposit/withdraw; wallet↔treasury via CharacterService.adjustWallet; all membership invariants and treasury guards), GuildController POST /api/guild/create + POST /api/guild/{id}/join + POST /api/guild/{id}/leave + GET /api/guild/{id} + POST /api/guild/{id}/deposit + POST /api/guild/{id}/withdraw (inline TokenHelper bearer auth + character ownership); V14__guilds.sql Flyway migration (Postgres DDL, UNIQUE indexes on guild_name + character_id, access-pattern index on guild_id); 10-scenario @SpringBootTest suite covering create+founder membership, duplicate name 400, already-in-guild create 400, already-in-guild join 400, join adds MEMBER, deposit wallet→treasury, withdraw founder→wallet, non-founder withdraw 400, treasury-insufficient withdraw 400, leave + founder-blocked-with-members 400.
- 2026-06-19 auction(AUCT): com.game.auction — Auction @Entity (auctions; auction_status/start_bid_gel/current_bid_gel/end_day/high_bidder_character_id, H2-safe column names), AuctionRepository (findByAuctionStatus/findBySellerCharacterId), AuctionService (create reserves goods via GoodsService.decrement or sets CellarItem.escrowed=true; bid validates OPEN+not-expired+not-self+amount-floor+wallet-affordability without charging; settle charges winner adjustWallet(-bid)+seller(+bid)+transfers item, or returns reserved item to seller on no-bid; lazy auto-settle in listOpen; idempotent double-settle), AuctionController POST /api/auction/create + GET /api/auction/open + POST /api/auction/{id}/bid + POST /api/auction/{id}/settle (inline TokenHelper bearer auth + character ownership); V15__auctions.sql Flyway migration (Postgres DDL + idx_auctions_auction_status + idx_auctions_seller_character_id); 10-scenario @SpringBootTest suite (CELLAR_ITEM escrow, GOODS reservation, bid raises currentBid, too-low bid 400, self-bid 400, settle-after-clock-advance pays seller+transfers goods to winner, no-bid settle returns item to seller, double-settle idempotent 200, lazy settle removes from open list, CELLAR_ITEM settle transfers ownership).
- 2026-06-19 mail(MAIL): com.game.mail — Mail @Entity (mailbox table; recipient_character_id/sender_character_id/attach_kind/attach_ref_id/attach_amount/is_read/is_claimed, all H2-safe column names), MailRepository (findByRecipientCharacterId/findByRecipientCharacterIdAndIsClaimedFalse), MailService (send with escrow-on-send for GEL/GOODS/CELLAR_ITEM; markRead; claim with idempotent 400 guard; delete blocked while unclaimed attachment present; system-mail helper), MailController POST /api/mail/send + GET /api/mail/{characterId} + POST /api/mail/{mailId}/read + POST /api/mail/{mailId}/claim + POST /api/mail/{mailId}/delete (inline TokenHelper bearer auth + character ownership); V17__mailbox.sql Flyway migration (Postgres DDL + idx_mailbox_recipient_character_id); 10-scenario @SpringBootTest suite (GEL send debits sender, inbox visibility, markRead, claim credits recipient + double-claim 400, GOODS attach claim, non-recipient 404, delete-blocked-by-unclaimed 400 + ok-after-claim, plain-mail claim 400, insufficient-GEL 400 + wallet unchanged, plain-mail delete 200).
- 2026-06-19 ranking(RANK): com.game.ranking — RankEntry record, RankingSnapshot @Entity (ranking_snapshots; board/subject_id/subject_name/score/rank_pos/sim_day/created_at; H2-safe, no `rank`/`value`/`year`), RankingSnapshotRepository (findByBoardOrderByRankPosAsc), RankingService (WEALTH=CharacterRepository.findAll sort walletGel desc; VINTNER=CellarItemRepository.findAll group by characterId take max quality sort desc; GUILD=GuildRepository.findAll sort by member-count desc tie-break treasury desc; meRank; snapshot persists rows), RankingController GET /api/ranking/wealth|vintner|guild + GET /api/ranking/me?board=&characterId= (ownership via TokenHelper) + POST /api/ranking/snapshot {board} (inline bearer auth); V18__ranking_snapshots.sql Flyway migration (Postgres DDL + idx_ranking_snapshots_board_rank_pos); 10-scenario @SpringBootTest suite (wealth ordered desc with rankPos 1-based, missing-auth 401, me-rank position, me absent→rankPos=0, ownership 404, invalid board 400, vintner board appears after grow, snapshot persists rows with correct fields, unknown snapshot board 400, guild board non-error).
- 2026-06-19 chat(CHAT): com.game.chat — ChatMessage @Entity (chat_messages; channel/sender_character_id/sender_name/body_text/created_at, H2-safe names), ChatMessageRepository (findByChannelOrderByCreatedAtDesc paged + findByChannelAndIdGreaterThanOrderByIdAsc polling), ChatService (4-type channel auth: GLOBAL=any-authed, REGION=homeRegion match, GUILD=GuildMemberRepository membership, DM=participant-id check; 500-char body guard; dmChannel(a,b) canonical key), ChatController POST /api/chat/send + GET /api/chat/{channel}?characterId=&sinceId= (inline TokenHelper bearer auth + ownership; GLOBAL readable without characterId), V20__chat_messages.sql Flyway migration (Postgres DDL + idx_chat_messages_channel_id + idx_chat_messages_channel_created_at); 9-scenario @SpringBootTest suite (GLOBAL send+get, REGION wrong-region 400, REGION correct-region 200, GUILD non-member 403, GUILD member 200, DM A/B read+C forbidden 403, empty body 400, 501-char body 400, sinceId polling returns only id>sinceId).
- 2026-06-19 festival(FEST): com.game.festival — FestivalDefinition POJO + FestivalCalendar (4 Georgian wine festivals: vine_blessing days 80–100 rewardGel 30, qvevri_opening days 130–150 rewardGel 40, rtveli days 240–270 rewardGel 75, new_wine_fair days 300–320 rewardGel 50; active(dayOfYear) filter), FestivalParticipation @Entity (festival_participations; season_year/festival_id/claimed/created_at, H2-safe), FestivalParticipationRepository (findByCharacterIdAndFestivalIdAndSeasonYear), FestivalService (getCalendar, getActive lazy-clock, participate with NOT_ACTIVE+404+ALREADY_PARTICIPATED guards + adjustWallet), FestivalController GET /api/festival/calendar + GET /api/festival/active + POST /api/festival/{festivalId}/participate (inline AccountTokenService bearer + CharacterService.getOwned ownership); V19__festival_participations.sql Flyway migration (Postgres DDL + idx_festival_participations_lookup on character_id/festival_id/season_year); 8-test @SpringBootTest suite (calendar 4 festivals+fields, active includes rtveli after clock steer, participate grants rewardGel, participate twice 400, NOT_ACTIVE 400 with code in body, unknown 404, ownership 404, no-double-pay wallet unchanged on second call).
- 2026-06-19 achievement(ACH): com.game.achievement — AchievementDefinition POJO + AchievementCatalog (6 milestones: first_estate/first_qvevri/master_vintner/wealthy_merchant/guild_founder/globetrotter; GEL+optional goods rewards), PlayerAchievement @Entity (player_achievements, unique(character_id,achievement_id), H2-safe columns), PlayerAchievementRepository, AchievementService (@Transactional unlock: ALREADY_UNLOCKED idempotent guard, adjustWallet+GoodsService.grant, currentAbsoluteDay→unlockedDay), AchievementController GET /api/achievement/catalog + GET /api/achievement/{characterId} + POST /api/achievement/{achievementId}/unlock (inline AccountTokenService bearer + CharacterService.getOwned); V21__player_achievements.sql Flyway (Postgres DDL + idx_player_achievements_character_id + uq_player_achievements_char_ach); 6-test @SpringBootTest suite (catalog 6 defs+fields, unlock records+grants rewardGel, double-unlock 400+ALREADY_UNLOCKED+wallet unchanged, GET list reflects unlocked, unknown 404, ownership 404/403); API.md §18 added.
- 2026-06-19 research(RES): com.game.research — ResearchNode record + ResearchCatalog (6 Georgian wine tech-tree nodes: improved_pruning 30 GEL/3d, temp_control 45 GEL/4d, rootstock_program 60 GEL/6d, logistics_network 40 GEL/3d, cold_soak 55 GEL/5d prereq=temp_control, barrel_program 70 GEL/7d prereq=temp_control), ResearchStatus constants, PlayerResearch @Entity (player_research; research_status/node_id/start_day/ready_day/created_at, H2-safe column names, UNIQUE(character_id,node_id)), PlayerResearchRepository, ResearchService (lazy-complete on read: RESEARCHING→COMPLETE when currentDay≥readyDay; startResearch: 404 unknown node, 400 double-start, 400 PREREQ_NOT_MET if prereqId not COMPLETE, adjustWallet debit, RESEARCHING row created), ResearchController GET /api/research/catalog + GET /api/research/{characterId} + POST /api/research/{nodeId}/start (inline AccountTokenService bearer auth + CharacterService.getOwned ownership); V23__player_research.sql Flyway migration (Postgres DDL + UNIQUE INDEX uq_player_research_char_node + idx_player_research_character_id); 8-test @SpringBootTest suite (catalog 6 nodes+fields, start debits wallet+RESEARCHING, prereq-not-met 400+PREREQ_NOT_MET body, clock-advance past readyDay→COMPLETE lazy, prereq-complete→dependent starts, double-start 400, insufficient-funds 400, ownership 404); API.md §21 added.
- 2026-06-19 contest(CONT): com.game.contest — Contest @Entity (contests; contest_status/end_day/prize_gel, H2-safe column names), ContestEntry @Entity (contest_entries; quality_score/placement nullable until judged, snapshots CellarItem.quality at entry time), ContestRepository (findByContestStatus), ContestEntryRepository (findByContestId/findByContestIdAndCharacterId), ContestService (create NPC-funded OPEN contest; listOpen lazily judges expired contests; enter with OPEN+not-expired+ownership+one-entry guards; judge sorts qualityScore desc/id asc tie-break, assigns placements 1..n, awards prizeGel to placement-1 via adjustWallet, idempotent), ContestController POST /api/contest/create + GET /api/contest/open + POST /api/contest/{id}/enter + POST /api/contest/{id}/judge + GET /api/contest/{id}/results (inline TokenHelper bearer auth + character ownership); V22__contests.sql Flyway migration (Postgres DDL + idx_contests_contest_status + idx_contest_entries_contest_id + idx_contest_entries_character_id); 8-test @SpringBootTest suite (create OPEN, enter snapshots quality, duplicate enter 400, enter after endDay 400, enter unowned item 404, two-character judge with different seeds/pickDays asserts higher-quality bottle owner has placement=1 and wallet+prizeGel, double-judge idempotent, lazy-judge removes expired from open list); API.md §20 added.
- 2026-06-19 tourism(TOUR): com.game.tourism — TourismLedger @Entity (tourism_ledgers; character_id UNIQUE, last_claim_day, created_at; H2-safe column names), TourismLedgerRepository, TourismService (lazy-create ledger at first access with lastClaimDay=currentDay; rate=BASE_PER_DAY(2.0)+PER_BUILDING(1.0)×buildingCount via BuildingRepository.findByOwnerCharacterId; getSnapshot returns {lastClaimDay,buildingsCount,currentRatePerDay,accruedSoFar=(currentDay−lastClaimDay)×rate}; claim credits wallet via adjustWallet+resets lastClaimDay; reads CharacterRepository for post-claim wallet balance), TourismController GET /api/tourism/{characterId}→snapshot + POST /api/tourism/{characterId}/claim→{paid,walletGel,lastClaimDay} (inline AccountTokenService bearer + CharacterService.getOwned ownership); V24__tourism_ledgers.sql Flyway (Postgres DDL + uq_tourism_ledgers_character_id); 7-test @SpringBootTest suite (lazy-create 0 buildings/rate=2.0/accrued=0, cottage raises rate by 1.0, advance 5 days accrued=10.0, claim pays 7×3.0+resets accrued to 0, second-claim pays 0, ownership GET→404, ownership claim→404).
- 2026-06-19 content(SCRIBE): wrote docs/CONTENT-BIBLE.md (world/tone, 5-phase progression arc Glekhi→Tavadi with wealth bands + sources/sinks, economy-balance tables tying 33 systems to phases + 6 mis-tune flags, 9 career PRO/CON specs, 7 region characters with real climate deltas, 12-NPC roster with authored dialogue); expanded com.game.quest QuestCatalog from 5→29 quests across the 5 phases (kept original 5 ids intact, reused PLANT_VINE/HARVEST/SELL_BOTTLES/CRAFT_VESSEL/VISIT + added FERMENT/SHIP/WIN_CONTEST/BUILD/RESEARCH/HIRE/TRADE/FOUND_GUILD/SPONSOR labels, all reward good ids verified against GoodsCatalog, NPC givers match roster); updated QuestControllerTest catalog-size assertion 5→29 (accept/complete flow + the 5 referenced quest ids unchanged).
- 2026-06-19 labor(LABOR): com.game.labor — StaffRole record + StaffCatalog (4 roles: vineyard_hand YIELD/30hire/5wage, cellar_master QUALITY/60hire/8wage, cooper_apprentice CRAFT/40hire/6wage, merchant_clerk SALES/50hire/7wage; all hireCostGel<=60), HiredStaff @Entity (hired_staff; labor_status/staff_type_id/hired_day/last_paid_day, H2-safe, idx_hired_staff_character_id), HiredStaffRepository (findByCharacterId/findByCharacterIdAndLaborStatus), LaborService (hire debits hireCostGel via adjustWallet; wage accrual lazy (currentDay-lastPaidDay)*dailyWage; payroll all-or-nothing CANNOT_MAKE_PAYROLL on shortfall, resets lastPaidDay; fire→QUIT stops accrual; benefits aggregate benefitType→sum ACTIVE only; CharacterRepository injected for post-payroll wallet read), LaborController GET /api/labor/catalog + GET /api/labor/{characterId} + POST /api/labor/hire + POST /api/labor/payroll + POST /api/labor/{staffId}/fire + GET /api/labor/benefits/{characterId} (inline AccountTokenService bearer + CharacterService.getOwned); V25__hired_staff.sql Flyway (Postgres DDL + idx_hired_staff_character_id); 12-scenario @SpringBootTest suite (catalog 4 roles, hire debits+ACTIVE, wagesOwed=0 same-day, wagesOwed=N×wage after advanceClock, payroll deducts+resets, payroll insufficient→400 wallet unchanged, fire→QUIT stops accrual+benefits, benefits aggregate, ownership 404, unknown role 404, hire insufficient 402).
- 2026-06-19 career(CAREER): com.game.career — CareerProfile record (careerType/summary/pro/con + 9 numeric multiplier fields, all 0.0 neutral; Builder factory sets only the field(s) a career uses), CareerProfileCatalog static map of all 9 CareerTypes with bible-exact multipliers (MERCHANT sellMarginMult=0.20, HAULER shippingDiscountMult=0.30, NEGOCIANT buyDiscountMult=0.20, GROWER yieldMult=0.15, WINEMAKER qualityMult=0.10; inferred: COOPER craftDiscountMult=0.20, BROKER brokerCommissionMult=0.05, NURSERYMAN cuttingMarginMult=0.25, ENOLOGIST gradeFeeIncomeMult=0.08), CareerController GET /api/career/catalog (all 9 profiles, bearer auth) + GET /api/career/{characterId} (owned character's profile, ownership via CharacterService.getOwned); no migration (static catalog, additive lane); 6-scenario @SpringBootTest suite (catalog size=9 all 9 names present, MERCHANT sellMarginMult=0.20, GROWER yieldMult=0.15, character MERCHANT profile, character HAULER profile, ownership 404, no-token 401 x2).
- 2026-06-19 regions(REGION): verified RegionSiteProfiles+RegionClimates+VineyardReplayService wiring complete (all done by prior RA lane); added RegionSimWiringTest (11 plain-JUnit tests: same SAPERAVI seed in KAKHETI vs RACHA_LECHKHUMI/IMERETI → different VineState+WineLot; KAKHETI/SAPERAVI byte-identical vs SimTestHelper canonical; RACHA+IMERETI determinism; estate path VineyardReplayService KAKHETI vs RACHA different view + KAKHETI determinism; KAKHETI SiteProfile canonical values guard; all-7-regions non-null; non-KAKHETI sites distinct); no production code changed; all existing tests unaffected.
- 2026-06-19 prestige(PRES): com.game.prestige — TitleLadder (GLEKHI/0, MEVENAKHE/50, MEURNE/200, AZNAURI/600, TAVADI/1500; all()/titleFor/nextTitle), PrestigeProfile @Entity (prestige_profiles; character_id UNIQUE/prestige/title_rank/updated_at, H2-safe), PrestigeService (lazy-create getOrCreate, awardPrestige amount>0 guard + titleFor recompute), PrestigeController GET /api/prestige/ladder (public) + GET /api/prestige/{characterId} (lazy-create, prestige/title/nextTitle/prestigeToNext) + POST /api/prestige/{characterId}/award (inline TokenHelper bearer+ownership, amount<=0→400); V28__prestige_profiles.sql Flyway migration (Postgres DDL + UNIQUE INDEX uq_prestige_profiles_character_id); 14-test @SpringBootTest + 13-test TitleLadderTest unit suite (ladder ordered/5-entries/GLEKHI-first, auto-create 0/GLEKHI, idempotent GET, award raises prestige, 60→MEVENAKHE, 250→MEURNE, nextTitle/prestigeToNext at 60=MEURNE/140, TAVADI nextTitle=null/toNext=0, amount=0→400, negative→400, ownership GET/award→404, no-token→401); API.md §23 added.
- 2026-06-19 travel(TRAVEL): com.game.travel — CharacterLocation @Entity (character_locations; travel_status/current_region/dest_region/depart_day/arrive_day/created_at, H2-safe column names, UNIQUE on character_id), CharacterLocationRepository (findByCharacterId), TravelService (getLocation: lazy-create at homeRegion SETTLED + lazy-arrive on TRAVELLING when currentAbsoluteDay>=arriveDay; depart: SETTLED guard, Region enum validation, same-region guard, GeoUtil.travelDays reuse, flat 5 GEL cost via adjustWallet INSUFFICIENT_FUNDS guard, TRAVELLING state set), TravelController GET /api/travel/{characterId} + POST /api/travel/{characterId}/depart (inline TokenHelper bearer auth + CharacterService.getOwned ownership); V26__character_locations.sql Flyway migration (Postgres DDL + UNIQUE INDEX uq_character_locations_character_id); 9-test @SpringBootTest suite (lazy-create SETTLED at KAKHETI, depart→TRAVELLING arriveDay>departDay, double-depart→400 ALREADY_TRAVELLING, unknown region→400, same region→400, clock-advance past arriveDay→SETTLED at destRegion, GeoUtil farther-region strictly-more-days, no-token→401, wallet debited exactly 5 GEL); API.md §22 already documented.
